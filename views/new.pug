include ./partials/header
include ./partials/nav

h2 Welcome to New Document

section( class='section_books' )

    form(action="/books/new", method="post")

        input(type="text", name="title", placeholder="Title ...")

        input(type="text", name="author", placeholder="Ahutor ...")

        input(type="text", name="image", placeholder="URL Image ...")

        textarea(name="description", cols="30", rows="10", placeholder="Description ...")

        input(type="submit", value="New Document")

